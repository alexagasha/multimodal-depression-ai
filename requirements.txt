numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
pandas>=2.2

# Real backbones (install when swapping mock encoders for real ones):
# torch>=2.2
# transformers>=4.40        # BERT, Wav2Vec2
# torchvision>=0.17         # MobileNetV3
# librosa>=0.10              # audio resampling / noise reduction
# mediapipe>=0.10            # face detection/cropping for video
# shap>=0.45                 # for richer XAI attribution later
