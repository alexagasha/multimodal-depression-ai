# Leave unset for local dev — vite.config.ts proxies /api to localhost:8000.
#
# Set this when the frontend and backend are deployed separately (e.g. the
# same Vercel-frontend / Render-backend split used by LASA-VOTING-SYSTEM):
# VITE_API_BASE_URL=https://your-backend.onrender.com
